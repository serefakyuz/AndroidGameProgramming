<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="serefakyuz.com.kitap"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="7" />

	<!-- İhtiyaç duyulan özellikleri aktifleştiren satırların eklenebileceği dizin burasıdır -->
	<!-- Aşağıda Wake Lock özelliğini aktifleştiren satırı eklemiş bulunmaktayız -->
	<uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name="android.oyun.gelistirme.YapayZekaUygulamasi"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>